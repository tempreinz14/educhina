{
  "name": "educhina",
  "version": "1.0.0",
  "description": "EduChina landing page",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": "18.x"
  }
}

